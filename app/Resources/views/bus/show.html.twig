{% extends 'backend/logistique/index.html.twig' %}

{% block content %}
    <h1>Bus</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bus.id }}</td>
            </tr>
            <tr>
                <th>Nbrplace</th>
                <td>{{ bus.nbrPlace }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bus_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bus_edit', { 'id': bus.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
