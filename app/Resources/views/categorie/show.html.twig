{% extends 'frontend/index.html.twig' %}
{% block login %}
{% endblock %}
{% block content %}
    <section class="banner_area">
        <div class="banner_inner d-flex align-items-center">
            <div class="overlay"></div>
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-lg-6">
                        <div class="banner_content text-center">
                            <h2>{{ categorie.nom }}</h2>
                            <p>In the history of modern astronomy, there is probably no one greater leap forward than
                                the
                                building and launch of the space telescope known as the Hubble.</p>
                            <div class="page_link">
                                <a href="index.html">Home</a>

                                <a href="{{ path('categorie_index') }}" >categories</a>
                                <a href="{{ path('categorie_show', { 'id': categorie.id }) }}" >{{ categorie.nom }}</a>

                            </div>

                            {% for livre in livres %}

                                <td>
                                    <div class="col-lg-50 col-md-30">
                                        <div class="single_fact">
                                            <div class="f_icon">
                                                <img src="img/f-icons/icon1.png" alt="">
                                            </div>
                                            <h4>{{ livre.titre }}</h4>

                                            <a href="{{ path('livre_show', { 'id': livre.id }) }}" class="btn btn-primary">Show</a>
                                            <p> celles-ci sont placés au-dessus de la liste des produits sous le H1. Je me demande si déplacer cette description en dessous de la page produit va changer quelque chose pour le référencement.
                                            </p>

                                        </div>
                                    </div>

                                    {% if loop.index is divisible by(4)	 %}
                                        <tr></tr>
                                    {% endif %}

                                </td>

                            {% endfor %}



                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
   
{% endblock %}
