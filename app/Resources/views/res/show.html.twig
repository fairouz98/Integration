{% extends 'base.html.twig' %}

{% block body %}
    <h1>Re</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ re.id }}</td>
            </tr>
            <tr>
                <th>R</th>
                <td>{{ re.r }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('res_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('res_edit', { 'id': re.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
